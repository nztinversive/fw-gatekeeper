# dlib source builds require these tools; install before the runtime lock.
# Build isolation is disabled so these exact, hash-verified versions are used.
setuptools==84.0.0
wheel==0.45.1
packaging==26.3
cmake==3.31.6
