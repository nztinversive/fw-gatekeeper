# FW Gatekeeper face service - pinned Python dependencies.
#
# Policy: every package is pinned with == so each Docker build produces the
# same runtime. Target: python:3.11-slim (CPython 3.11, x86_64, glibc 2.36).
# numpy MUST stay <2 and match the kiosk pin so server and kiosk embeddings
# are produced by identical numeric stacks.
#
# Wheel availability for cp311 x86_64 was verified (2026-09-05) with:
#   pip download --only-binary=:all: --platform manylinux2014_x86_64 \
#     --platform manylinux_2_28_x86_64 --python-version 3.11 \
#     --implementation cp --no-deps <pkg>==<ver>
#
# To re-pin: `pip freeze` inside a known-good image, copy the versions here,
# re-run the pip download check above, and rebuild the image.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
numpy==1.26.4
Pillow==12.3.0
opencv-python-headless==4.11.0.86
onnxruntime==1.29.0
